/** Audit logging placeholder — ActivityLog model removed in B2B schema. */
export async function logActivity(
  _action: string,
  _entity: string,
  _entityId?: string,
  _details?: string,
  _userId?: string
) {
  // no-op
}
